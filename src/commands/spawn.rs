// `grove spawn` — full impl lands in T9.

use colored::Colorize;

pub fn run(_name: &str, _branch: Option<&str>, _task: Option<&str>) {
    eprintln!(
        "{} grove spawn not yet implemented on this branch (T9).",
        "Note:".yellow()
    );
    std::process::exit(1);
}
