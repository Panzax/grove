// `grove integrate` — full impl lands in T12.

use colored::Colorize;

pub fn run(_into: Option<&str>, _no_test: bool) {
    eprintln!(
        "{} grove integrate not yet implemented on this branch (T12).",
        "Note:".yellow()
    );
    std::process::exit(1);
}
